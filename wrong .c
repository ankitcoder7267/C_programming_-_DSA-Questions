#include<stdio.h>

    int minium(int , int ) ;
    

int main()
{
        int c, d;
        scanf("%d %d", &c, &d);
        
        printf("Smaller no is %d", minium(c,d));
        return 0;
}
    int minium (int a, int b);
    
    
        if(a < b)
        {
            z = a;
        }
        else
        {
            z = b;
        }
        return z ;
}
